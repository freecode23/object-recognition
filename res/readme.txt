For all task, the command line argument should be a full path including the training csv_file:
"./src/vidDisplay res/train/label_train.csv"

To train or collect data, the command line argumnets are folder to  put the images and csv file:
"./src/vidDisplay res/train/" or "./src/vidDisplay res/validate/"